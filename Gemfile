# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem 'numo', '0.1.1'
gem 'matplotlib', '1.0.0'
gem 'red-datasets', '0.0.7'
